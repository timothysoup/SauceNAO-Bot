# Sauce NAO Discord Bot

This bot tries to find the sauce of images in a Discord channel.

## Installation

1. Clone the repository
2. Install dependencies `npm install`
3. Create a `.env` file in the root directory with `example.env` as a template
4. Run with `npm start`
